## tsp migration

### Breaking Changes

  - Model `Condition` renamed its instance variable `values` to `values_property`
  - Model `PatchObject` moved instance variable `enabled` under property `properties`

### Other Changes

  - Deleted model `AlertProcessingRulesList`/`ManagedResource`/`ResourceAutoGenerated` which actually were not used by SDK users


# Release History

## 1.0.0b1 (2026-01-19)

### Other Changes

  - Initial version